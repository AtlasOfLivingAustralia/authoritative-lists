df = pd.DataFrame({'A': ['a', 'b', 'c', 'a', 'c']})
mapping = {'a': 1, 'b': 2, 'c': 3}


import pandas as pd

df = pd.DataFrame({'A': ['a', 'b', 'c', 'a', 'c']})
mapping = {'a': 1, 'b': 2, 'c': 3}
You can create a new column B in df based on the values in column A and the mapping dictionary using map():

python
Copy code
df['B'] = df['A'].map(mapping)
default_value = 0
df['B'] = df['A'].map(mapping).fillna(default_value)
